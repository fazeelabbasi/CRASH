#include "Client.h"
#include <stdio.h>

int main(int argc, char** argv) {
	//if (argc != 2) {
	//	printf("usage: %s server-name\n", argv[0]);
	//	return 1;
	//}

	//Client* c = new Client();
	//c->start(argv[1]);
	//c->sendInfo();
	//c->receiveInfo();
	//c->stop();

	return 0;
}